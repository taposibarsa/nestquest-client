NEXT_PUBLIC_API_URL=http://localhost:5000

# Google OAuth is configured on the API (nestquest-server/.env).
# In Google Cloud Console → OAuth 2.0 Client, authorized redirect URI must be:
#   http://localhost:5000/api/auth/google/callback
